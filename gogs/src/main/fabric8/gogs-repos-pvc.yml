apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gogs-repositories
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  volumeName: gogs-repositories
status:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 100Mi