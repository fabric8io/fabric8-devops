---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "hubot"
  labels:
    provider: "fabric8"
    project: "${project.artifactId}"
    expose: "true"
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    name: "hubot-slack"
